version: 2
jobs:
  unshallow:
    steps:
      - checkout
      - run: if [ -e .git/shallow ]; then git fetch --unshallow; fi

  build:
    docker:
      - image: circleci/ubuntu-server:latest

    branches:
      only:
        - circle-ci-20

    steps:
      - checkout
      - run: prove

workflows:

  version: 2

  buildall:
    jobs:
      - build
