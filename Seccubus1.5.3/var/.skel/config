# Any parameter in in the main configuration can be overwritten on a per scan
# basis the parameters below are the most likely candidates for modification
#MODE=full		# For Nessus
#MODE=full-openvas	# For OpenVAS
#MODE=nikto		# For local Nikto scanner
#NIKTOOPTS=''           # Additional nikto options (-o -h and -F) are set by 
			# default
#EMAIL=""
#PRESCAN=""
#POSTSCAN=""

# The parameters below can also be modifed on a per scan basis, but require some
# more tought
#HOST=''				# This is the host running your nessusd
#PORT=1241				# Nessusd port (1241 = default)
#USER=''				# Nessusd user name
#PASS=''				# Nessusd password
#NESSUSBIN=/usr/bin/nessus		# The full path to your nessus client
#NESSUSBIN=/usr/bin/OpenVAS-Client	# Use this line if you are using OpenVAS
#NIKTOBIN=/usr/bin/nikto.pl             # The full path to nikto.pl on your
                                        # system
#HOME=/home/seccubus			# Home location of seccubus
#VAR=$HOME/var				# This is where the 'database' lives
#SENDMAIL=/usr/sbin/sendmail		# Path to sendmail
